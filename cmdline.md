# Jargonlang commandline

# Commands
## `-v` or `--version`
Prints the current version of the jargonlang compiler
## `-D` or `--debug-line`
Compiles the given line, printing the output for each compilation step